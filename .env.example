# Google Workspace APIs & Authentication
# Used for Google Sheets, Google Drive, and client-side Google Sign-In.
# Ensure this API key is authorized for Google Sheets API and Google Drive API in your Google Cloud Console.
VITE_GOOGLE_API_KEY=your_google_api_key_here

# Google OAuth 2.0 Client ID
# Used for Google Sign-In to authorize access to user's data (Sheets, Drive).
# Create this in Google Cloud Console -> APIs & Services -> Credentials.
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Google Sheet ID
# The ID of the Google Sheet where data will be stored.
# Extract this from your Google Sheet URL (e.g., https://docs.google.com/spreadsheets/d/SHEET_ID_HERE/edit)
VITE_GOOGLE_SHEETS_ID=your_google_sheet_id_here

# Google Drive Folder ID
# The ID of the Google Drive folder where files will be uploaded.
# Extract this from your Google Drive folder URL (e.g., https://drive.google.com/drive/folders/FOLDER_ID_HERE)
VITE_GOOGLE_DRIVE_FOLDER_ID=your_google_drive_folder_id_here

# Google Cloud Vision API
# Used for PDF OCR processing.
# Ensure this API key is authorized for the Cloud Vision API in your Google Cloud Console.
# This can often be the same as VITE_GOOGLE_API_KEY if that key is unrestricted
# or specifically enabled for the Cloud Vision API.
VITE_GOOGLE_CLOUD_API_KEY=your_cloud_api_key_here

# Google Cloud Project ID
# The Project ID associated with your Google Cloud Vision API.
# Find this in your Google Cloud Console dashboard.
VITE_GOOGLE_CLOUD_PROJECT_ID=your_google_cloud_project_id_here

# Note: Currency conversion is handled manually via the UI. No API key is needed for this.